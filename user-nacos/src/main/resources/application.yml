server:
  port: 8021
spring:
  application:
    name: user-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
        namespace: public
        ephemeral: true #默认为true（临时实例），宕机后会剔除